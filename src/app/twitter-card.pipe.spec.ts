import { TwitterCardPipe } from './twitter-card.pipe';

describe('TwitterCardPipe', () => {
  it('create an instance', () => {
    const pipe = new TwitterCardPipe();
    expect(pipe).toBeTruthy();
  });
});
